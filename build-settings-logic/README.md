These build settings are duplicated from the [ktor repository](https://github.com/ktorio/ktor/tree/main/build-settings-logic).

It is best to continue copying these files instead of editing directly.
