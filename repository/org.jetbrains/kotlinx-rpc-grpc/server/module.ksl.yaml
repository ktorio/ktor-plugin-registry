platform: jvm
gradle:
  plugins:
    - $libs.plugins.kotlinx.rpc.grpc
dependencies:
  - ../core
  - $libs.kotlinx.rpc.grpc.ktorServer
  - $libs.grpc.netty
testDependencies:
  - $libs.kotlinx.rpc.grpc.client
  - $ktorLibs.server.testHost
sources:
  - target: slot://org.gradle/gradle/buildRoot
    text: |-
      rpc {
          protoc()
      }
