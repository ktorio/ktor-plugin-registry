platform: jvm

dependencies:
  - $ktorLibs.server.jetty

amper:
  application:
    mainClass: io.ktor.server.jetty.jakarta.EngineMain

sources:
  - target: slot://org.gradle/gradle/buildRoot
    text: |-
      application {
          mainClass = "io.ktor.server.jetty.jakarta.EngineMain"
      }