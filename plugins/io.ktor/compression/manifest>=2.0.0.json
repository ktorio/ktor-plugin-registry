{
  "id": "compression",
  "name": "Compression",
  "version": "2.0.0",
  "ktor_version": "2.0.0",
  "short_description": "Compress outgoing content using gzip, deflate or custom encoder and thus reduce the size of the response",
  "github": "https://github.com/ktorio/ktor/blob/4517e3856a3cae8f157d2f6966635217827d2abd/ktor-server/ktor-server-core/jvm/src/io/ktor/features/Compression.kt",
  "copyright": "Apache 2.0",
  "vendor": {
    "name": "Ktor",
    "url": "https://ktor.io/"
  },
  "group": "HTTP",
  "install_recipe": {
    "imports": [
      "io.ktor.server.plugins.compression.*"
    ],
    "install_block": "install(Compression) {\n    gzip {\n        priority = 1.0\n    }\n    deflate {\n        priority = 10.0\n        minimumSize(1024) // condition\n    }\n}"
  },
  "dependencies": [
    {
      "group": "io.ktor",
      "artifact": "ktor-server-compression-jvm",
      "version": "$ktor_version"
    }
  ],
  "documentation": {
    "description": "The [Compression](https://ktor.io/docs/compression.html) plugin allows you to compress outgoing content. You can use different compression algorithms, including `gzip` and `deflate`, specify the required conditions for compressing data (such as a content type or response size), or even compress data based on specific request parameters.",
    "usage": "You can configure compression in multiple ways: enable only specific encoders, specify their priorities, compress only specific content types, and so on. For example, To enable only specific encoders, call the corresponding extension functions:\n```kotlin\ninstall(Compression) {\n    gzip()\n    deflate()\n}\n```\nThe code snippet below shows how to compress all text subtypes and JavaScript code using `gzip`:\n```kotlin\ninstall(Compression) {\n    gzip {\n        matchContentType(\n            ContentType.Text.Any,\n            ContentType.Application.JavaScript\n        )\n    }\n}\n```\nYou can learn more from the [Compression](https://ktor.io/docs/compression.html) help topic.",
    "options": ""
  }
}