{
  "id": "routing",
  "name": "Routing",
  "version": "2.0.0",
  "ktor_version": "2.0.0",
  "short_description": "Allows to define structured routes and associated handlers.",
  "github": "https://github.com/ktorio/ktor/blob/4517e3856a3cae8f157d2f6966635217827d2abd/ktor-server/ktor-server-core/jvm/src/io/ktor/routing/Routing.kt",
  "copyright": "Apache 2.0",
  "vendor": {
    "name": "Ktor",
    "url": "https://ktor.io/"
  },
  "group": "Routing",
  "install_recipe": {
    "imports": [
      "io.ktor.server.routing.*",
      "io.ktor.server.response.*"
    ],
    "test_imports": [
      "io.ktor.client.request.*",
      "io.ktor.client.statement.*",
      "io.ktor.server.testing.*",
      "kotlin.test.*",
      "io.ktor.http.*"
    ],
    "templates": [
      {
        "position": "in_routing",
        "text": "get(\"/\") {\n    call.respondText(\"Hello World!\")\n}"
      },
      {
        "position": "test_function",
        "text": "@Test\nfun testRoot() = testApplication {\n    application {\n        configureRouting()\n    }\n    client.get(\"/\").apply {\n        assertEquals(HttpStatusCode.OK, status)\n        assertEquals(\"Hello World!\", bodyAsText())\n    }\n}"
      }
    ]
  },
  "dependencies": [
    {
      "group": "io.ktor",
      "artifact": "ktor-server-core-jvm",
      "version": "$ktor_version"
    }
  ],
  "documentation": {
    "description": "[Routing](https://ktor.io/docs/routing-in-ktor.html) is the core Ktor plugin for handling incoming requests in a server application. When the client makes a request to a specific URL (for example, `/hello`), the routing mechanism allows us to define how we want this request to be served.",
    "usage": "The Routing plugin can be installed by calling the `routing` function. After installing the Routing plugin, you can call the `route` function inside `routing` to define a route:\n```kotlin\nrouting {\n    route(\"/hello\", HttpMethod.Get) {\n        handle {\n            call.respondText(\"Hello\")\n        }\n    }\n}\n```\n\nKtor also provides a series of functions that make defining route handlers much easier and more concise. For example, you can replace the previous code with a get function that now only needs to take the URL and the code to handle the request:\n\n```kotlin\nrouting {\n    get(\"/hello\") {\n        call.respondText(\"Hello\")\n    }\n}\n```\n\nTo learn more, see the [Routing](https://ktor.io/docs/routing-in-ktor.html) section.",
    "options": ""
  }
}