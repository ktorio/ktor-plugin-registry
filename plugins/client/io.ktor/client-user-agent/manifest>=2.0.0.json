{
    "id": "client-user-agent",
    "name": "UserAgent",
    "version": "2.0.0",
    "ktor_version": "2.0.0",
    "short_description": "Adds a User-Agent header to all requests",
    "github": "https://github.com/ktorio/ktor/blob/main/ktor-client/ktor-client-core/common/src/io/ktor/client/plugins/UserAgent.kt",
    "copyright": "Apache 2.0",
    "vendor": {
        "name": "Ktor",
        "url": "https://ktor.io/"
    },
    "group": "HTTP",
    "install_recipe": {
        "imports": [
            "io.ktor.client.plugins.*"
        ],
        "install_block": "install(UserAgent) {\n    agent = \"Ktor client\"\n}"
    },
    "documentation": {
        "description": "The [UserAgent](https://api.ktor.io/ktor-client/ktor-client-core/io.ktor.client.plugins/-user-agent/index.html) plugin adds a `User-Agent` header to all requests.",
        "usage": "To install `UserAgent`, pass it to the `install` function inside a [client configuration block](create-client.md#configure-client). Then, use the `agent` property to specify the `User-Agent` value:\n\n```kotlin\nimport io.ktor.client.*\nimport io.ktor.client.engine.cio.*\nimport io.ktor.client.plugins.*\n// ...\nval client = HttpClient(CIO) {\n    install(UserAgent) {\n        agent = \"Ktor client\"\n    }\n}\n```\n\nKtor also allows you to add a browser- or curl-like `User-Agent` value using corresponding functions:\n\n```kotlin\nval client = HttpClient(CIO) {\n    BrowserUserAgent()\n    // ... or\n    CurlUserAgent()\n}\n\n```",
        "options": ""
    },
    "target": "client"
}